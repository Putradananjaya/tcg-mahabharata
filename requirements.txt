# Pinned exactly from `venv/` (Python 3.9), verified against `tests/test_imports.py`
# (38/38 passed) on 2026-09-17. Regenerate with:
#   python3 -m venv venv && source venv/bin/activate
#   pip install -r requirements.txt
#   python -m pytest tests/
#
# Direct dependencies (imported by name in src/, experiments/, scripts/, tests/):
fastapi==0.128.8       # src/api/server.py
uvicorn==0.39.0        # scripts/*, src/api/server.py runtime
pydantic==2.13.4       # src/api/server.py request/response models
numpy==2.0.2           # simulator, metrics, optim, surrogate, sensitivity
matplotlib==3.9.4      # experiments/*, figures/ generation
pandas==2.3.3          # experiments/visualize.py, experiments/advanced_visualize.py (legacy scripts, see REPRODUCE.md)
pytest==8.4.2          # tests/

# Transitive dependencies (pinned for full reproducibility, do not import directly)
Pygments==2.21.0
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.12.1
click==8.1.8
contourpy==1.3.0
cycler==0.12.1
exceptiongroup==1.3.1
fonttools==4.60.2
h11==0.16.0
idna==3.18
importlib_resources==6.5.2
iniconfig==2.1.0
kiwisolver==1.4.7
packaging==26.2
pillow==11.3.0
pluggy==1.6.0
pydantic_core==2.46.4
pyparsing==3.3.2
python-dateutil==2.9.0.post0
pytz==2026.3.post1
six==1.17.0
starlette==0.49.3
tomli==2.4.1
typing-inspection==0.4.2
typing_extensions==4.16.0
tzdata==2026.4
zipp==3.23.1

# Explicitly NOT a dependency: scipy, scikit-learn, SALib, torch, xgboost, lightgbm.
# src/sensitivity/sobol.py, src/metrics/nonparametric.py, src/metrics/nash_averaging.py,
# src/optim/baselines.py, and src/surrogate/baselines.py implement their own from-scratch
# versions specifically because this venv has none of these installed — see the
# "no scipy/sklearn in this venv" comments in those files. Do not add these libraries
# without also revisiting those from-scratch implementations.
